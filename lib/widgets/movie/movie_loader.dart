import 'package:flutter/material.dart';

class MovieLoader extends StatelessWidget {
  const MovieLoader({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
